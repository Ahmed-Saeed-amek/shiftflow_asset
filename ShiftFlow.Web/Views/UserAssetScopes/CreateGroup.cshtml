@model GroupAssetScopeViewModel
@{ ViewData["Title"] = Loc.T("New group scope"); }
<partial name="_PageHeader" model="@(new PageHeaderModel{
    Title = Loc.T("New group scope"),
    Breadcrumbs = new List<(string Label, string? Url)>{ (Loc.T("Asset Visibility"), Url.Action("Index")), (Loc.T("New group scope"), null) } })" />
<partial name="_GroupForm" model="Model" />
